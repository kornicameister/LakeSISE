(deftemplate lake
    "Lake describes our environment - in status before iteration"
    (slot width)
    (slot length))

(deftemplate lake-weather
    (slot pressure)
    (slot rain)
    (slot storm))
