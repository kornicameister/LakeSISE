(deftemplate poacher
    (slot x)
    (slot y)
    (slot weapon-range)
    (slot weapon-power)
    (slot captured)
    (slot caughtFishes))

(deftemplate poacher-out
    (slot caught-fish)
    (slot caught-fish-count)
    (slot x)
    (slot y)
    (slot is-caught))

(deftemplate forester
    (slot bribeThreshold)
    (slot player))